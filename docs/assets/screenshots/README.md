# README Screenshots

These screenshots are intentional GitHub/release assets promoted from visual smoke output.

Refresh process:

```bash
npm run screenshots
```

Then review `test-results/screenshots/` and replace only stable assets:

- `ailu-home.png`
- `ailu-ai-workspace.png`
- `ailu-llm-library.png`
- `ailu-responsive.png`

Do not commit raw Playwright output or screenshots that contain API keys, private paths or local user data.
